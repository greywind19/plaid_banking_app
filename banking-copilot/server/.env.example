# Plaid sandbox credentials — get these from https://dashboard.plaid.com (free)
# Never commit the real .env file.
PLAID_CLIENT_ID=your_client_id_here
PLAID_SECRET=your_sandbox_secret_here
PLAID_ENV=sandbox

# REST API port (the React UI calls this)
PORT=8787

# Sandbox institution + user used to bootstrap demo data.
# ins_109508 = "First Platypus Bank" (Plaid's standard sandbox test institution)
SANDBOX_INSTITUTION_ID=ins_109508
